import HomepageClient from "./HomepageClient";

export const metadata = {
  title: "Zyphron Cloud — Staff Portal",
  description: "Staff Management & Operations Portal for Zyphron Cloud — Active, Offline & Whole Team",
};

export default function Page(){
  return <HomepageClient />;
}
